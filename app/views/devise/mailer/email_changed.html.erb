<!DOCTYPE html>
<html>
    <head>
        <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
    </head>
    <body>
        <p><%= t('.salutation', display_name: @resource.display_name) %></p>

        <% if @resource.try(:unconfirmed_email?) %>
            <p><%= t('.first_para', email: @resource.unconfirmed_email) %></p>
        <% else %>
            <p><%= t('.first_para', email: @resource.email) %></p>
        <% end %>

        <%= t('.footer_html') %>
    </body>
</html>
