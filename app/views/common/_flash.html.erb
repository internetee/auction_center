<% unless flash.empty? %>
  <div class="ui message flash" style='display: block; margin-bottom: 20px;'>
    <% flash.each do |key, value| -%>
      <% if ['notice', 'alert'].include?(key) %>
        <div class="header <%= key %>" id="flash"><%= h value %></div>
      <% end %>
    <% end %>
  </div>
<% end %>
