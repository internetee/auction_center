<%= turbo_stream.update "mini",
                         html: Money.from_amount(auction.min_bids_step, Setting.find_by(code: 'auction_currency').retrieve) %>

<%= turbo_stream.update "current_#{auction.id}_price",
                         partial: 'english_offers/current_price',
                         locals: { auction: auction, user: nil } %>

<%= turbo_stream.update "auction_timer",
                         partial: 'english_offers/timer',
                         locals: { auction: auction } %>
