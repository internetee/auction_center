en:
  okcomputer:
    check:
      passed: "%{registrant_name}: PASSED %{message}"
      failed: "%{registrant_name}: FAILED %{message}"
  health_checks:
    index:
      title: Site health check
      test: Check name
      status: Check status
      message: Message
      default: Application
      database: Database
      api: Auctions API
      email: Email integration
      registry: Registry integration
      sms: SMS service integration
      tara: TARA service integration
      status_ok: OK
      status_fail: FAIL
